# ==================== INSTRUCTIONS ====================
# 1. Copy this file and rename it to ".env"
# 2. Update the values below with your actual configuration
# 3. Keep the .env file secret - never commit it to version control
# =======================================================

# ==================== APPLICATION ====================

# Application name (shown in header and page titles)
APP_NAME=ChibiBooru

# ==================== SECURITY ====================

# Password for web UI login (REQUIRED - change this!)
APP_PASSWORD=your-secure-password-here

# Secret key for Flask sessions (REQUIRED - use a long random string)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here

# System control secret for API operations (REQUIRED - change this!)
SYSTEM_API_SECRET=your-system-api-secret-here

# ==================== API KEYS ====================

# SauceNao API key for reverse image search (optional)
# Get your key from: https://saucenao.com/user.php
SAUCENAO_API_KEY=

# Gelbooru API credentials (optional)
# Get your credentials from: https://gelbooru.com/index.php?page=account&s=options
GELBOORU_API_KEY=
GELBOORU_USER_ID=

# ==================== AI TAGGING ====================

# Local AI tagger display name (shows in metadata and UI)
# Examples: "CamieTagger", "WD14", "Z3D-E621"
LOCAL_TAGGER_NAME=CamieTagger

# Run local tagger on ALL images (even when online sources are found)
# If "true": Runs on every image for prediction storage and data augmentation
# If "false": Only runs as fallback when no online sources are found (default)
LOCAL_TAGGER_ALWAYS_RUN=false

# ==================== WEB SERVER ====================

# Flask host (0.0.0.0 allows external connections)
FLASK_HOST=0.0.0.0

# Flask port number
FLASK_PORT=5000

# Flask debug mode (set to "true" for development, "false" for production)
FLASK_DEBUG=false

# ==================== SIMILARITY ====================

# Similarity calculation method
# Options: 'jaccard' (basic set intersection/union), 'weighted' (IDF + category weights - default)
SIMILARITY_METHOD=weighted

# ==================== UPSCALER (AI IMAGE UPSCALING) ====================

# Enable AI image upscaling feature (disabled by default)
# When enabled, adds an upscale button to the image page action bar
UPSCALER_ENABLED=false

# Upscaler model to use
# Options: 'RealESRGAN_x4plus' (general), 'RealESRGAN_x4plus_anime' (anime-specific)
UPSCALER_MODEL=RealESRGAN_x4plus

# Upscale factor (the x4plus models are designed for 4x)
UPSCALER_SCALE=4

# Tile size for processing large images (lower = less VRAM needed)
UPSCALER_TILE_SIZE=512
